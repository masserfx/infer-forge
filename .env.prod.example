# INFER FORGE - Produkční environment konfigurace
# Zkopíruj tento soubor jako .env.prod a vyplň skutečné hodnoty

# ============================================
# Database (PostgreSQL 16 + pgvector)
# ============================================
POSTGRES_DB=infer_forge
POSTGRES_USER=infer
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here_min_32_chars

# ============================================
# Redis (Celery broker & cache)
# ============================================
REDIS_PASSWORD=CHANGE_ME_redis_password_min_32_chars

# ============================================
# Application Security
# ============================================
# Generuj pomocí: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE_ME_random_64_char_string_use_secrets_token_urlsafe

# ============================================
# CORS (Comma-separated allowed origins)
# ============================================
# Příklady:
# - Development: http://localhost:3000
# - Production: https://infer-forge.example.com,https://www.infer-forge.example.com
CORS_ORIGINS=https://your-domain.example.com

# ============================================
# AI/ML (Anthropic Claude API)
# ============================================
ANTHROPIC_API_KEY=

# ============================================
# Email - IMAP (příjem pošty)
# ============================================
IMAP_HOST=
IMAP_PORT=993
IMAP_USER=
IMAP_PASSWORD=

# ============================================
# Email - SMTP (odesílání pošty)
# ============================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=

# ============================================
# Pohoda XML integrace (Stormware)
# ============================================
# URL mServer API pro import XML do Pohody
POHODA_MSERVER_URL=

# ============================================
# Monitoring (volitelné)
# ============================================
# Sentry DSN pro error tracking
SENTRY_DSN=

# ============================================
# Poznámky
# ============================================
# 1. Nikdy necommituj .env.prod s reálnými hesly do Git
# 2. Používej silná hesla (min. 32 znaků, random)
# 3. V produkci nastav správné CORS_ORIGINS (ne localhost)
# 4. Pro generování SECRET_KEY použij:
#    python -c "import secrets; print(secrets.token_urlsafe(64))"
# 5. Pravidelně rotuj hesla (doporučení: každé 3 měsíce)
# 6. Backup databáze denně (viz docs/deployment.md)
